apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: auth
  name: auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth
  template:
    metadata:
      labels:
        app: auth
    spec:
      containers:
        - image: 265989594936.dkr.ecr.eu-west-1.amazonaws.com/auth:latest
          name: auth
          env:
            - name: HTTP_PORT
              value: '3001'
            - name: TCP_PORT
              value: '3002'
            - name: JWT_EXPIRATION_TIME
              value: '3600'
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt
                  key: secret
            - name: MONGODB_URI
              value: 'mongodb://ngimdockzemfack:szJiZ1nBqSuXKc05@ac-ppube2q-shard-00-00.5jcz2wu.mongodb.net:27017,ac-ppube2q-shard-00-01.5jcz2wu.mongodb.net:27017,ac-ppube2q-shard-00-02.5jcz2wu.mongodb.net:27017/?ssl=true&replicaSet=atlas-6b15ir-shard-0&authSource=admin&retryWrites=true&w=majority'
          ports:
            - containerPort: 3001
            - containerPort: 3002
